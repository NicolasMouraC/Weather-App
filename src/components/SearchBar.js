import React from "react";

const SearchBar = () => {
    return (
        <div className="header">
            <div className="logo">
                Logo
            </div>

            <div className="search-bar">
                SearchBar
            </div>
        </div>
    );
}

export default SearchBar;